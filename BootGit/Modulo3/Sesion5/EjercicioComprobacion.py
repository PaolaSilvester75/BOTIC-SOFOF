# Importamos el módulo math para usar su función factorial
import math

# Asignamos el número para el que queremos calcular el factorial
numero = 4

# Calculamos el factorial usando la función factorial del módulo math
factorial = math.factorial(numero)

# Imprimimos el resultado
print(f"El factorial de {numero} es: {factorial}")
