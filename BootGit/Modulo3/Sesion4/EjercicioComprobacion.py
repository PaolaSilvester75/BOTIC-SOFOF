# Creamos las variables
numero_entero = 8
numero_decimal = 10.5
palabra = "ejercicio"

# Creamos un set que contiene las variables anteriores
mi_set = {numero_entero, numero_decimal, palabra}

# Creamos una lista que contiene el set y una variable con el valor lógico False
mi_lista = [mi_set, False]

# Imprimimos la lista en pantalla
print(mi_lista)
